summon z as com;
summon c as com;
summon m as float;
summon i as com;
summon x as int;
summon b1 as bool, summon b2 as bool;

arise Pixel {
summon r as float;
summon ii as float;
};

summon p as Pixel;

summon iterate as void (float limit) {
    c = 0.35 + 0.4i;
    z = 0.0 + 0.0i;

    x = 0;
    while (x < 10) {
        z = z * z + (c + 5+2i);
        
        m = mag(z + 0+2i);
        
        if (m > limit) {
            Print("m is too big");
            x = 100; 
        }
        
        x = x + 1;
    }
};

Hello, world!

b1= 5 < 4;
b2= b1 && b2;
b1 = !T;
z = 0.00+5i;
z = 3.0 + 4.0i;

z = 10 +0i * 0+ 2.5i;
z = (3+2i) / 2.0 +0i;

m = real(5 + 2i);
m = imag(0+10i);

r of p = real(z );
ii of p = imag(z );

Print("Magnitude is:");
Print(mag(3+4i));

iterate(2.0);


Goodbye, world!