summon x as int;
summon b1 as bool;
summon b2 as bool;
summon m as float;
summon z as com;

// Feature: Function declaration without body
summon protoFunc as int(int a);

summon testShadow as void () {
    summon x as float;
    x = 5.5;
    Print(x); 
};

Hello, world!
    x = 999;
    
    // Existing shadowing test
    Print(x);       
    testShadow();    
    Print(x);       

    // Feature: Unary minus on variable
    x = -x; 
    Print(x);

    // Feature: Modulo operator
    x = 10 % 3;
    Print(x);

    // Feature: Not equal operator
    b1 = (x =/= 5);
    Print(b1);

    // Feature: Greater or equal operator
    b2 = (x >= 1);
    Print(b2);

    // Feature: Complex imag() function
    z = 1.0 + 2.5i;
    m = imag(z);
    Print(m);

Goodbye, world!
