summon z as com;
summon c as com;
summon m as float;
summon i as com;
summon x as int;
summon b1 as bool, summon b2 as bool;
summon st as string;

arise Pixel {
summon r as float;
summon ii as float;
summon draw as com (Pixel p1, Pixel p2){
    summon xyz as float;
    };
};

summon p as Pixel;

summon iterate as void (float limit) {
    c = 0.35 + 0.4i;
    z = 0.0 + 0.0i;

    x = 0;
    while (x < 10) {
        z = z * z + (c + 5+2i);
        
        m = mag(z + 0+2i);
        
        if (m > limit) {
            Print(m);
            x = 100; 
        }
        
        x = x + 1;
    }
};

summon manyparams as com (int a, int b, int d, float ab, float bc, com xy, com xz, string abcxz);

Hello, world!

b1= 5 < 4;
b2= b1 && b2;
b1 = !T;
z = 0.00+5i;
z = 3.0 + 4.0i;
z= 0                +           2i;
x=-x;
b1= (x<=x) ;
b1 = b2 || (3 <4+5) && !b2;
st= "abcdefgh" + "xyz";

z = 10 +0i * 0+ 2.5i;
z = (3+2i) / 2.0 +0i;

m = real(5 + 2i);
m = imag(0+10i);

r of p = real(z );
ii of p = imag(z );

Print("Magnitude is:");
Print(mag(3+4i));

iterate(2.0);


Goodbye, world!